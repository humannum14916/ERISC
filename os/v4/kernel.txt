
meta header kernel;

define array[char] message
  = "Hello from ERISC OS v4!";

function stackless void MAIN(){
  define int index = 0;
  define char temp = null;
  while(message[index] != {char}0){
    temp = message[index];
    asm "TRS MAIN.temp,TTY";
    index = index + 1;
  };
};
